apiVersion: apps/v1
kind: Deployment
metadata:
  name: openwebui
  namespace: ai-stack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openwebui
  template:
    metadata:
      labels:
        app: openwebui
    spec:
      containers:
      - name: openwebui
        image: ghcr.io/open-webui/open-webui:latest
        ports:
        - containerPort: 8080
        env:
        - name: OLLAMA_BASE_URL
          value: http://ollama:11434
        # Register the RAG API as an additional OpenAI-compatible connection.
        # After applying, go to OpenWebUI → Settings → Connections and it will
        # appear, or select "rag-documents" in the model picker to chat with docs.
        - name: OPENAI_API_BASE_URLS
          value: "http://rag-api:8000/v1"
        - name: OPENAI_API_KEYS
          value: "rag-not-needed"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            cpu: "1"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 15
          failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: ai-stack
spec:
  selector:
    app: openwebui
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
